// components/AddAllButton.js
import React from 'react';

const AddAllButton = () => {
  // Handle add all logic here (simulated)
  const handleAddAll = () => {
    console.log('Adding all songs to queue (simulated)');
  };

  return (
    <button onClick={handleAddAll}>
      Add All
    </button>
  );
};

export default AddAllButton;
